# 超星自动答题脚本 · 题库接入与修复总结

> 最后更新：2026-09-11（第二次更新：**接入网课小工具题库 + 双题库回退链**）
> 涉及项目：`Samueli924/chaoxing` v3.1.4（冻结版）
>
> **当前状态**：`config.ini` 里 `provider = TikuIcodef,TikuAnevol` —— 两个题库都已内置，
> 先问免费快的（icodef），搜不到再问付费慢的（ANEVOL）。实测 3/3 命中，见第十二章。

---

## 一、背景与目标

用开源项目 `Samueli924/chaoxing`（v3.1.4）自动刷超星网课。原版只支持内置的几个题库（TikuYanxi / TikuLike / TikuAdapter / AI / SiliconFlow），目标是把**自建的 ANEVOL 题库 API** 接进去，实现自动查题、自动提交，同时保证视频能多线程并行播放。

---

## 二、环境

| 项目 | 值 |
|---|---|
| 系统 | Windows（本机**没有可用的系统 Python**，`WindowsApps\python` 是商店占位符） |
| 构建解释器 | `C:\Users\11977\.workbuddy\binaries\python\envs\default\Scripts\python.exe`（3.13.x） |
| 打包工具 | PyInstaller 6.22.2 |
| 运行配置 | `config.ini`（本目录） |
| 日志 | 程序运行目录下的 `chaoxing.log` |
| 交付 exe | `chaoxing-3.1.4-fixed.exe` |

> 因为本机没有系统 Python，所有可执行方案都得**打包成 exe** 交付。

---

## 三、此前已完成的三项修复

> ⚠️ 这三项是**上一轮**的成果，仍然有效。本轮新增的「双题库接入 + 题库链 + 题号修复」见**第十二章**。

### 1. 内置 ANEVOL 题库（免桥接）

在 `api/answer.py` 里新增 `TikuAnevol(Tiku)` 类并加入 `__all__`，配置 `provider = TikuAnevol` 即可直接用，**不需要再开 `anevol_bridge.exe`**。

> 原版 v3.1.4 只认内置的那几个类名，写别的名字会直接 `KeyError`。

### 2. worker 线程自愈

- 原代码的 `log_error` 装饰器是 `except BaseException: raise`，任何异常都会把 worker 线程彻底掀掉，导致"线程数越来越少"、"一次只能看两条视频"。
- 已在 `main.py::worker_thread` 里把 `process_chapter` 包 try/except，异常转 `ChapterResult.ERROR` 走重试；并新增 `watchdog_thread`（每 3s 检查、补位，上限 `jobs*4`）。

### 3. 字体表打包修复

`api/cxsecret_font.py::resource_path()` 改为三级回退（`_MEIPASS` → exe 同目录 → 当前目录），打包时加 `--add-data "resource/font_map_table.json;resource"`，解决题干乱码。

**自测结果**：

| 自测脚本 | 结果 |
|---|---|
| `selftest_anevol.py` | **8/8 PASS**（答案映射链路） |
| `selftest_anevol_errors.py` | **12/12 PASS**（错误路径 + 熔断） |
| `selftest_worker.py` | **5/5 PASS**（注入 6 次 ReadTimeout + 4 并发 12 章 → 12/12 成功、worker 全程 4/4 存活） |

---

## 四、两个误解的澄清（都靠实证推翻）

### ① "只有一个线程了" ≠ 线程死了

- 看门狗告警数为 **0**（`检测到 worker 线程退出` 一次都没出现），证明 4/4 线程一直健在。
- 写日志分析脚本（`analyze_concurrency.py`）按"登录成功"切分每轮运行、统计视频并发分布，发现：
  - 13:24 那轮官方 exe 因 2 个线程被 ReadTimeout 打死，确实 77% 时间只有 2 个并发（**这是原版 bug，修复版已解决**）；
  - 16:04 那轮"只剩 1 个视频"是**正常的** —— 课程快做完了，前一章的视频早播完了，自然只剩最后一个。
- **结论**：并发视频数 = Σ(当前在跑章节里未完成的视频任务点)，不是固定值。

### ② "答题挤占了视频线程" ≠ 成立

- 代码上，视频任务点**完全不调用题库**：`process_job` 只对 `type == "workid"` 的题目调 `tiku.query`。
- 但**同一个 worker 是按章节分配的**，`process_chapter` 内会 `executor.map` 等该章**所有**任务点（含答题）跑完才释放 worker。所以答题慢（超时重试）会**间接拖住**该 worker 继续拿下一章的视频 —— 这是"章节级串行"的设计，不是线程被题目占用。

---

## 五、核心故障定位：ANEVOL 上游 402（服务商欠费）—— **已恢复**

> ✅ **2026-09-11 复查：ANEVOL 已恢复正常。** 用真 token 实测 `1+1等于几？`，
> 6.7 秒返回 `{"answer":"B","code":1,"model":"xiaomi:mimo-v2.5"}`。下面的 402 排查过程
> 保留作为「以后再遇到同类故障」的判据。

用户反馈"题库查询一直失败、频率高得不正常"。写了两个探针脚本实测：

**`工具脚本/probe_anevol.py`** —— 鉴权矩阵对比（真/假 token × query 参数 / X-API-Key 头 / 无鉴权 / GET / 无 type）：

- 文档写的 `X-API-Key` 头，和 `?token=query 参数` **等价有效**（只有假 token 才返回 401）
- 真 token 请求返回 **HTTP 500**

**`工具脚本/anevol_quickcheck.py`** —— 单发快速探活，服务端原样返回：

```
HTTP 500  耗时 23.5s
{"code":0,
 "detail":"主模型和所有备用模型都失败了。最后的错误: 调用AI API时出错:
           AI API调用失败: HTTP 402 - {\"code\": 30001,
           \"message\": \"Sorry, your account balance is insufficient\", \"data\": null}",
 "msg":"AI服务暂时不可用，请稍后重试"}
```

**铁证结论**：

- `HTTP 402 = Payment Required`，上游原话是 **"your account balance is insufficient"（账户余额不足）**
- 关键推理：**密钥无效返回 401，次数不足也返回 401**；拿到 500 + 402 说明**你的 key 有效、次数够用**，是 ANEVOL 拿去调它背后 AI（主模型 + 所有备用模型）时**它自己的账单爆了**
- 因此与你的 token、`config.ini`、本机程序、请求格式**全部无关**。ANEVOL 恢复之前，任何代码都拿不到答案。

---

## 六、顺带修掉的两个真实 bug

排查过程中发现原错误处理有缺陷，一并修了并重新打包。

### bug① 错误响应字段漏读

原 `_ask` 只读响应体里的 `msg` 字段，**漏了 `detail`**。而 ANEVOL 的**服务端错误（HTTP 500）恰恰把真实原因放在 `detail` 里**（`msg` 只是笼统的"AI服务暂时不可用"）。结果：真实错误被当成"未知原因"，重试短路和熔断逻辑**全部失效**，每题傻跑 3 次 ≈ 77 秒。

→ 改为 `_reason_from_json()` 依次读 `msg / detail / message / error`。

### bug② 熔断器不分故障类型

重做为分级策略：

| 故障类型 | 行为 |
|---|---|
| 致命（402 等付费/额度问题） | 熔断 **600s**，不重试 |
| 普通服务故障 | 连续 5 次触发熔断，时长逐级翻倍 **120→240→480→900s** |
| 读超时（>40s） | 立即终止，不重试 |
| 题不在库里（HTTP 200 + code:0） | **不计入**熔断（正常业务结果） |

相关常量：

```python
_ANEVOL_READ_TIMEOUT = 40
_ANEVOL_FATAL_PAUSE  = 600
_ANEVOL_CB_THRESHOLD = 5
_ANEVOL_CB_PAUSE_BASE = 120
_ANEVOL_CB_PAUSE_MAX  = 900
```

其他相关改动：

- `api/base.py::SessionManager` 超时与重试重设：
  `Retry(total=3, connect=3, read=2, status=0, backoff_factor=0.6)` + 连接池 32 + `timeout=(5, 20)`
- `_ask` 最多 2 次尝试；区分 `service`（服务故障）/ `fatal`（致命）标志，**只有服务级故障计入熔断**

**修复效果验证**（16:27 新实例日志）：`ANEVOL 熔断中，本次跳过查询` 出现 **95 次**，后续题目全部秒级跳过，不再每题傻等 77 秒。

---

## 七、"不会自动提交"是设计保护，不是 bug

提交逻辑（`api/base.py::study_work`）：

```
cover_rate = found / total * 100
若 submit=true 且 cover_rate >= 0.8（或 rollback_times>=1） → pyFlag=""  (提交)
否则                                                      → pyFlag="1" (仅保存)
```

故障期覆盖率是 **0%**（全随机作答），所以 `提交成功` = 0 次，全是"保存成功"。

**这是正确行为** —— 覆盖率不足时提交 100% 瞎猜的答案，大概率答错，有的课还会因正确率过低触发风控。

---

## 八、关闭题库模式

把 `config.ini` 的 `[tiku]` 段里 `provider` **留空**即可：

```ini
provider =
```

代码路径：`provider` 为空 → `get_tiku_from_config()` 里 `if not cls_name: raise KeyError` → `Tiku.DISABLE = True` → `api/base.py:584` 的 `if self.tiku.DISABLE or not self.tiku: return StudyResult.SUCCESS` 使**整个答题流程直接跳过**，不发任何查题请求。

**效果**：只做视频/文档任务点，题目不答、不提交，也不会再有 ANEVOL 报错刷屏。

**恢复**：把 `provider =` 改回 `provider = TikuAnevol`，或用 `备份配置\config.ini.bak_tiku_on` 覆盖（那是 ANEVOL 全套配置）。

> 注意：改配置后**必须重启 exe** 才生效（配置只在启动时读一次）。

---

## 九、目录说明

```
网课\
├── chaoxing-3.1.4-fixed.exe     ★ 主用：修复版（内置 ANEVOL、线程自愈、字体修复）
├── chaoxing-3.1.4.exe             原版（保留作退路）
├── anevol_bridge.exe              Plan B 桥接程序（用 TikuAdapter 方案时才需要）
├── start_chaoxing.bat           ★ 一键启动（双击即可）
├── config.ini                   ★ 当前配置（provider = TikuIcodef,TikuAnevol = 双题库已开启）
├── config-bridge.ini              Plan B 配置（TikuAdapter + 本地桥接）
├── 工具脚本\
│   ├── anevol_quickcheck.py       单发探活：验证 ANEVOL 是否恢复
│   ├── probe_anevol.py            鉴权矩阵探针（对比 token 位置/请求方式）
│   ├── analyze_concurrency.py     日志分析：统计视频并发分布
│   ├── analyze_startup.py         日志分析：对比开局节奏
│   └── probe_result.txt           probe 实测结果存档
├── 源码\
│   ├── chaoxing-fixed\            修复版完整工程（api/、main.py、resource/、自测脚本）
│   │   ├── selftest_anevol.py           ANEVOL 答案映射链路自测（8 项）
│   │   ├── selftest_anevol_errors.py    ANEVOL 错误路径/熔断自测（12 项）
│   │   ├── selftest_worker.py           worker 线程自愈自测（5 项）
│   │   ├── selftest_icodef.py           ★ 新增：网课小工具题库自测（18 项，含 1 并发串行化验证）
│   │   ├── selftest_chain.py            ★ 新增：题库链回退 + 题号预处理自测（10 项）
│   │   ├── selftest_ai_fallback.py      ★ 新增：AI 兜底自测（12 项）
│   │   └── smoketest_live.py            ★ 新增：真实网络冒烟（会消耗 ANEVOL 额度）
│   └── anevol-bridge\             桥接程序源码
└── 备份配置\
    ├── config.ini.bak_tiku_on                  关闭题库前（ANEVOL 全套）★ 最常用
    ├── config.ini.bak_before_fixed_exe         部署修复版之前
    ├── config.ini.bak_20260910                 最初版本
    └── chaoxing-3.1.4-fixed.exe.bak_20260911   ★ 本轮替换下来的上一版 exe（回退用）
```

> **本轮部署方式**：替换时根目录那个旧 exe 正被两个实例占用（PID 24740 / 25856，19:22 启动），
> Windows 不允许覆盖运行中的 exe，所以先把旧 exe **重命名**进 `备份配置\`（重命名不影响正在运行的进程），
> 再把新 exe 放到原文件名。两个运行中的实例全程未受影响，跑完即可；下次双击 `start_chaoxing.bat` 即为新版。

**一键启动**：双击 `start_chaoxing.bat`。它内部用 `%~dp0`（脚本自身目录）定位 exe 和配置，**整个文件夹搬到哪都能用**。

---

## 十、避坑清单（给后续排查的人）

1. **不要怀疑 token/配置**：拿到 500 + 402 就是服务商欠费，别浪费时间改代码。
2. **错误信息在 `detail` 不在 `msg`**：ANEVOL 错误体字段不统一 —— 业务失败（HTTP 200 + code:0）用 `msg`，服务端错误（HTTP 500）用 `detail`。
3. **鉴权有两种等效写法**：`X-API-Key` 头和 `?token=` 参数等价；假 token 返回 401。
4. **原版的坑**：`log_error` 装饰器会让异常掀掉整个 worker 线程；题库类名必须严格匹配内置名字，否则 `KeyError`。
5. **打包坑**：`resource/font_map_table.json` 必须显式 `--add-data`，否则题干乱码。
6. **本机无系统 Python**：所有可执行方案要打包成 exe。
7. **章节级串行**：答题慢会拖住同 worker 的下一章视频，这是设计如此，不是 bug。
8. **重启才生效**：改 `config.ini` 后必须重启 exe。
9. **中文路径 bat 要 GBK 编码**：否则 cmd（默认代码页 936）会找不到文件。
10. **网课小工具题库限「1 并发」**：它返回 `code:-2 触发流控限制: 1并发限制` 不是坏了，是并发超了；
    程序内部已强制串行（类级锁），别去把这个锁去掉。
11. **`code:-1` ≠ 服务故障**：那是「题不在库里」，属于正常业务结果，**不能**计入熔断，
    否则一片冷门题就会把题库停掉几分钟。
12. **多题库用逗号写**：`provider = TikuIcodef,TikuAnevol`，顺序即优先级；
    写错类名只会跳过那一个并在日志里报错，不会整个题库挂掉。
13. **`get_tiku_from_config()` 只负责构造**：真正的加载在随后的 `init_tiku()`，
    自己写脚本调题库时两步都要调（`main.py` 里就是这么写的）。

---

## 十一、当前状态与后续选项

**已完成**：内置 ANEVOL、worker 自愈、字体表打包、错误解析修复、分级熔断器、**双题库接入 + 题库链 + 题号修复**，
全部通过 48 项自测 + 真实网络冒烟（3/3 命中）。

**已解决**：ANEVOL 上游 402 故障已自行恢复（见第五章开头的复查记录）。

**后续可选**：

1. 想更省钱：把 `provider` 只留 `TikuIcodef`（免费），ANEVOL 完全不用
2. 想更高命中率：改成 `TikuAnevol,TikuIcodef`（先花 ANEVOL 的钱，再有 icodef 兜底）
3. 覆盖率不达标时强制提交（**有风险**：随机答案可能挂科 / 触发风控，不建议）
4. 再扩题库：照 `TikuIcodef` 的写法加一个类，然后把类名加进 `provider` 用逗号连起来即可

---

## 十二、本轮新增：网课小工具题库接入 + 双题库回退链

> 起因：把你给的这份题库 JSON 配置接进 `Samueli924/chaoxing`。

```json
[
  { "name": "ANEVOL题库API开放平台", "url": "https://tiku.anevol.cn/api/search?token=…",
    "method": "get", "data": { "title": "${title}", "options": "${options}", "type": "${type}" },
    "handler": "…code===1 → [question, answer]…" },
  { "name": "网课小工具题库（GO题）", "url": "https://cx.icodef.com/wyn-nb?v=4",
    "method": "post", "data": { "question": "${title}" },
    "handler": "return res.code===1?[undefined,res.data]:[res.msg,undefined]" }
]
```

那份 JSON 是**油猴/网课助手**（OCS 那套 `GM_xmlhttpRequest` + `handler`）的配置格式，
里面有 `${title}` 占位符和 **JavaScript 的 handler 函数** —— chaoxing 是纯 Python 项目，
没法直接执行 JS handler。所以做法是：**把这两个数据源按 chaoxing 的题库类接口各实现一个原生类**，
再用「题库链」把两者串起来。语义和你的 JSON 完全一致，且不需要任何本地桥接进程。

### 12.1 JSON 配置 → 代码 的对照

| 你 JSON 里的东西 | 落地实现 |
|---|---|
| ANEVOL：`GET/POST /api/search?token=…`，body `{title, options, type}` | `api/answer.py::TikuAnevol`（上一轮已内置，本轮保持） |
| ANEVOL handler 的 `code===1 → answer` | `TikuAnevol._ask()` 里判 `code == 1` |
| 网课小工具：`POST /wyn-nb?v=4`，body `{question}` | **本轮新增** `api/answer.py::TikuIcodef` |
| 网课小工具 handler 的 `code===1 → res.data` | `TikuIcodef._ask()` 里判 `code == 1` 取 `data` |
| 多个题库「按顺序问，第一个有答案就用」 | **本轮新增** `api/answer.py::TikuChain` |
| `data` 里多选题用 `#` 连接 | `_normalize_text_answer()` 按 `#`/换行拆开后再交回 chaoxing |

### 12.2 实测挖出来的三个坑（都已处理）

**坑①：网课小工具官方限「1 并发」，而 chaoxing 默认 `jobs = 8`。**

实测并发请求会直接被限流：

```json
{"code":-2,"data":"李恒雅忙不过来啦,触发流控限制: 1并发限制"}
```

处理：`TikuIcodef` 用**类级锁把所有请求整体串行化**（锁一直握到拿到响应为止，
否则等于没限并发），并在被限流时退避 2 秒重试，最多 3 次。
自测里用 6 个线程并发调用，实测**并发数恒为 1**。

**坑②：`code` 的含义不止「成功/失败」两种。**

| 返回 | 含义 | 处理 |
|---|---|---|
| `code:1` + 有内容 | 命中，`data` 就是答案 | 直接用 |
| `code:-1` | **题不在题库里**（正常业务结果） | 返回 None，**不计入熔断**，交给下一个题库 |
| `code:-2` | 触发流控（服务级故障） | 退避重试，计入熔断 |
| HTTP 5xx | 基础设施抖动 | 重试 3 次；连续 5 题失败 → 熔断 60s（逐级翻倍至 300s） |
| HTTP 401/403/404 | 鉴权/路径问题，重试无意义 | 直接放弃 |

> 「题不在库里」绝不能计入熔断 —— 否则一片冷门题会把整个题库停掉 2 分钟，
> 这是上一轮 ANEVOL 那边已经踩过一次的坑（见第六章 bug②）。

**坑③：两个题库返回的答案形式完全不同。**

- ANEVOL 对选择题返回的是**选项字母**（`"B"`），必须还原成选项原文再交回 chaoxing，
  否则 `clean_res()` 会把字母删成空串，`is_subsequence("", 选项)` 恒为真 → **永远选 A**。
- 网课小工具返回的是**选项原文**（如 `"《诗经》"`），多选题是 `#` 连接的多段文本；
  但原文里可能带顿号/逗号，又会让 `check_single()` 判成「多段答案」而丢弃。

处理：把两个题库共用的归一化逻辑抽成 `_AnswerNormalizeMixin`
（`_clean_option` / `_split_options` / `_parse_letters` / `_normalize_judgement` / `_safe_single`
/ `_normalize_text_answer`），`TikuAnevol` 与 `TikuIcodef` 都继承它。
其中 `_normalize_text_answer()` 还带了一个小闸门：**只有答案里不含中文时**才尝试按字母解析，
免得把 `COVID-19` 这种明文答案误当成选项字母。

### 12.3 顺带修掉的原版 bug：题号预处理把题干削坏了

`api/answer.py::Tiku.query` 原来用 `^\d+` 去题号：

```python
q_info['title'] = sub(r'^\d+', '', q_info['title'])     # 原版
```

它会把 **`"1+1等于几？"` 削成 `"+1等于几？"`**、把 **`"1921年中国共产党成立"` 削成 `"年中国共产党成立"`**
—— 题干一变，题库自然搜不到。这个 bug 是本次真实冒烟测试里抓到的（icodef 因此漏了一道本该 0.2 秒命中的题）。

改为「数字后面必须跟标点，且数字不超过 3 位」：

```python
q_info['title'] = sub(r'^\s*\d{1,3}\s*[.、,，)）:：]\s*', '', q_info['title'])
```

`"1. 下列说法正确的是"` 照样能去掉题号，而 `"1+1等于几？"`、`"1949. 中华人民共和国成立"` 原样保留。

### 12.4 配置怎么改

`config.ini` 的 `[tiku]` 段（**改完必须重启 exe 才生效**）：

```ini
; 从左到右依次询问，第一个查到答案的胜出，后面的不再请求
provider = TikuIcodef,TikuAnevol

; ---- 网课小工具题库（GO题）----
icodef_url = https://cx.icodef.com/wyn-nb?v=4   ; 留空=用默认值
icodef_min_interval = 0.3                        ; 串行前提下的最小间隔(秒)
icodef_timeout = 15                              ; 读超时(秒)
icodef_authorization =                           ; 可选，公开接口不填也能用

; ---- ANEVOL 题库 ----
url = https://tiku.anevol.cn/api/search
tokens = e1aa9a…7958ebde                         ; 你的密钥
anevol_min_interval = 0.3
```

常用改法：

| 想要的效果 | provider 写成 |
|---|---|
| **默认**：省额度又快（icodef 优先，ANEVOL 兜底） | `TikuIcodef,TikuAnevol` |
| 命中率优先（先花 ANEVOL，icodef 兜底） | `TikuAnevol,TikuIcodef` |
| 完全免费，不用 ANEVOL | `TikuIcodef` |
| 只用 ANEVOL | `TikuAnevol` |
| 关闭题库（只刷视频/文档） | 留空 |

### 12.5 验证结果

| 自测 | 结果 |
|---|---|
| `selftest_icodef.py`（新增） | **18/18 PASS** — 答案映射 9 例 + 错误路径/熔断 8 项 + **并发数恒为 1** |
| `selftest_chain.py`（新增） | **10/10 PASS** — 顺序回退、省钱（命中后不再问下一个）、异常隔离、错类名跳过、题号预处理 |
| `selftest_anevol.py`（回归） | **8/8 PASS**（重构后无退化） |
| `selftest_anevol_errors.py`（回归） | **12/12 PASS** |
| `selftest_worker.py`（回归） | **5/5 PASS** |
| `smoketest_live.py`（真实网络） | **3/3 命中** |

真实网络实测耗时（同一批题）：

| 题目 | 题库链（icodef→ANEVOL） | 只用 icodef | 只用 ANEVOL |
|---|---|---|---|
| 单选 | **0.2s** | 0.4s | 9.2s |
| 多选 | **0.5s** | 2.6s | 13.4s |
| 填空 | **2.6s** | 0.5s | 7.5s |

> 题库链比「只用 ANEVOL」快 **10~20 倍**，这就是把 icodef 放前面的意义。

### 12.6 打包与部署

- 打包：`python -m PyInstaller --noconfirm --clean chaoxing-3.1.4-fixed.spec`
  （用 `C:\Users\11977\.workbuddy\binaries\python\envs\default\Scripts\python.exe`，PyInstaller 6.22.2）
- 部署：新 exe 已放到根目录 `chaoxing-3.1.4-fixed.exe`（28,906,425 字节），
  上一版留在 `备份配置\chaoxing-3.1.4-fixed.exe.bak_20260911`。
- **怎么确认 exe 里真的装进了新代码**（不运行 exe、不联网、不碰账号）：

  ```powershell
  & "C:\Users\11977\.workbuddy\binaries\python\envs\default\Scripts\python.exe" `
    "工具脚本\verify_exe_modules.py" "chaoxing-3.1.4-fixed.exe"
  ```

  它会解包 exe 内嵌的 PYZ 归档并列出符号，输出 `[OK] TikuIcodef / TikuChain / ...` 即表示新代码已封进去。
  （用它对比过新旧 exe：新版全 `OK`，旧版这几项全是「缺失」。）

### 12.7 回退办法

- **临时回退**：把 `config.ini` 的 `provider` 改成 `TikuAnevol`（或留空 = 关闭题库），重启 exe。
- **回退整个程序**：把 `备份配置\chaoxing-3.1.4-fixed.exe.bak_20260911` 复制回根目录改名成
  `chaoxing-3.1.4-fixed.exe` 即可（复制前先关掉正在运行的实例）。
- **更彻底**：`chaoxing-3.1.4.exe`（原版）一直在目录里没动过。
- **Plan B 桥接方案**：`config-bridge.ini` + `anevol_bridge.exe` 也原样保留。

---

## 十三、AI 兜底：两个题库都没答案时，交给大模型来答

> 需求原话：「两个题库都没找到，或者由于其他原因失败的题目，由你来回答。」
> 结论：**能，而且题库链天然支持** —— 把 AI 挂在链尾即可。

### 13.1 一句话原理

题库链的语义就是「按顺序问，前面全都给不出答案才轮到下一个」。所以：

```ini
provider = TikuIcodef,TikuAnevol,AI
```

这样**任何**让前两个库交白卷的情况都会落到 AI 手上，而不是像以前那样随机瞎填：

| 失败原因 | 以前 | 现在 |
|---|---|---|
| 题不在题库里（`code:-1`） | 随机作答 | **AI 回答** |
| ANEVOL 读超时（>40s） | 随机作答 | **AI 回答** |
| 题库服务故障 / 熔断中 | 随机作答 | **AI 回答** |
| 返回的答案与题型对不上、被校验丢弃 | 随机作答 | **AI 回答** |

### 13.2 必须先做的一件事：填 API Key

AI 兜底要真正调用模型，所以**必须**在 `config.ini` 的 `[tiku]` 段补上 key：

```ini
endpoint = https://api.deepseek.com/v1     ; DeepSeek 官方接口
key = sk-你的key                            ; ← 申请：https://platform.deepseek.com/api_keys
model = deepseek-chat
min_interval_seconds = 3                    ; 8 个 worker 并发时的最小请求间隔
http_proxy =                                ; 需要代理才填，例如 http://127.0.0.1:7890
```

**没填 key 会怎样**：程序启动时日志里会出现一行明确的

```
AI 兜底未配置完整（缺少 key），已跳过 AI 兜底。请在 config.ini 的 [tiku] 段补齐 endpoint / key / model。
```

然后退回「icodef → ANEVOL」两段式，其余功能不受影响。**不会**出现「每题都发一次注定失败的请求」这种浪费。

### 13.3 对原版 `AI` 类的三处加固

原版 `AI` 类是给「单独使用 AI 答题」设计的，直接拿来当兜底有三处不够可靠，本轮补齐：

1. **配置不全时初始化即明确报错并跳过。**
   原实现是每题都去 `OpenAI(base_url='', api_key='')`，失败了只丢一句
   「无法解析大模型输出内容」—— 排查时根本看不出是没配 key。
2. **请求间隔加锁 + 增加熔断。**
   `jobs = 8` 时多个 worker 会并发答题，原实现的间隔判断没有锁，等于没限流。
   现在：间隔判断加锁；连续 5 次失败熔断 300 秒；鉴权/余额类错误（HTTP 401/402/403
   或 "insufficient / 余额 / 额度 / 无效" 等关键字）直接熔断 600 秒并不再重试。
3. **答案归一化与其它题库保持一致。**
   大模型返回的可能是选项原文、也可能是字母，还可能把 `Answer` 写成字符串而不是列表
   （原实现用 `"\n".join()` 会把字符串拆成单个汉字）。现在统一走 `_normalize_text_answer()`，
   并且喂给模型的选项已经去掉 `A.` / `A、` 标号。

> 附带重构：`_AnswerNormalizeMixin`（公共归一化混入类）从 ANEVOL 段落**上移**到 `Tiku` 基类之后，
> 这样 `AI` 也能继承它。这是纯代码搬移，`TikuAnevol` / `TikuIcodef` 行为不变（回归自测全绿）。

### 13.4 ⚠️ 一个必须知道的行为变化

**AI 答的题会被当成「已搜到」计入覆盖率。** 于是：

```
覆盖率 = (题库+AI 答出的题) / 总题数
覆盖率 >= 80% 且 submit = true  →  自动交卷
```

以前覆盖率低（全随机），章节只保存不提交；**现在覆盖率会被 AI 拉高，章节会真的被提交**，
AI 答错的题也会一起交上去。这是你确认过的选择（保持 `submit = true`）。

如果哪天想改稳一点，把 `config.ini` 里的 `submit` 改成 `false` 即可（只保存，事后自己核对再交）。

### 13.5 验证

| 自测 | 结果 |
|---|---|
| `selftest_ai_fallback.py`（新增） | **12/12 PASS** |

覆盖的场景：题库命中时完全不调用 AI（省钱）｜两个库都空时 AI 接管｜AI 返回字母/原文/多选/判断的归一化｜
AI 输出乱码不崩｜没配 key 时初始化即停用并被移出链｜401 报错只试 1 次并熔断 600s｜连续 5 次失败熔断 300s｜
喂给模型的选项已去标号。

全部自测合计 **63 项断言，0 失败**。

### 13.6 成本

DeepSeek `deepseek-chat` 的单价约 ￥1~2 / 百万 token，一道单选题（题目+选项+回答）大约几百 token，
**一道题不到 1 分钱**；而且只有前两个题库都没答案的题才会调用它。按时长计费的课，一学期通常也就几毛到几块钱。

### 13.7 日志整改：没查到 ≠ 调用失败

**起因**：用户看到日志里成片的

```
ERROR | api.answer:query:240 - 从网课小工具题库获取答案失败：1【单选题】……
ERROR | api.answer:_ask:1267 - ANEVOL 查询失败 -> code=0 | 没有找到相关答案
```

以为题库/程序坏了 —— 实际上那只是**链路中间环节没查到**，后面 ANEVOL/AI 早把答案答出来并提交了。

**原则**：`没查到`（正常业务结果）不再用 ERROR；只有**真正的故障**和**全线失守**才报 ERROR。

| 情况 | 级别 | 文案 |
|---|---|---|
| 题库没收录该题 | INFO | `网课小工具题库未收录该题 -> …` |
| 链上某一环没答案（中间过程） | DEBUG | `X 未命中，继续问下一个题库：…` |
| 链上某一环发生服务故障 | WARNING | `X 本次未查成，已交给下一个题库 -> …` |
| **链路最终命中** | INFO | `题库链命中：ANEVOL题库` |
| **链路全部失守**（唯一该看的 ERROR） | ERROR | `题库链全部未命中（没查到答案），该题将走随机作答：…` |
| 额度/鉴权类问题 | ERROR | `ANEVOL 拒绝请求(额度/付费问题) -> …`（这个必须管） |

实现要点：`TikuChain` 给每个子题库打上 `_in_chain` 标记，`Tiku.query` 据此决定
「中间环节」记 DEBUG 还是「单独使用」记 INFO；链自身用 `_is_chain` 标记来输出那条唯一的总结 ERROR。

另外把「题不在题库里」和「密钥/套餐有问题」区分开了：ANEVOL 返回 `code:0` 时按原因文案判断，
含「没有找到 / 未找到 / 未收录」→ INFO；含「密钥 / 无效 / 余额 / 额度」→ ERROR。
（判断顺序很重要：`_ANEVOL_NOTFOUND_WORDS` 必须排在 `_ANEVOL_NO_RETRY_WORDS` 之前，
否则「答案不存在」会被当成「密钥不存在」报成 ERROR。）

**实测效果**（`selftest_chain.py` 第 10 组用例，现在会直接断言日志级别）：

```
DEBUG 原始标题：日志题A
INFO  网课小工具题库未收录该题 -> 日志题A
DEBUG 网课小工具题库未命中，继续问下一个题库：日志题A
INFO  从ANEVOL题库获取答案：日志题A -> 中国共产党的领导
INFO  题库链命中：ANEVOL题库
INFO  从题库链获取答案：日志题A -> 中国共产党的领导
```

**0 条 ERROR。** 而全链都没答案时，恰好 1 条 ERROR 并明确写出「该题将走随机作答」。

---

## 十四、考试：可行性报告 + 只读「考试看板」

> 需求：「学习通有视频、章节答题和考试，做完视频和章节答题后才能考试，能不能把自动考试也加上。」
>
> **结论：技术上能做到（有开源实现），但需要绕过反作弊与身份核验，本项目不做；
> 只实现了零风险的「考试看板 + 截止提醒」。**

### 14.1 考试和章节测验是两套完全不同的接口

| | 章节测验（本程序已支持） | 考试 |
|---|---|---|
| 接口族 | `mooc-ans/work/addStudentWorkNew` | `exam-ans/exam/**`（**手机端 API**） |
| 请求数 | 一次 POST | 多步 SSR 流程 |
| 次数 | 可重做 | **通常只有一次机会** |
| 影响 | 平时分 | **期末成绩** |

### 14.2 真实考试流程（源码 + 实测佐证）

参考实现：[jexjws/CxKitty](https://github.com/jexjws/CxKitty)（GPL-3.0，作者 SocialSisterYi —— 正是本项目 `CacheDAO` 注释里致谢的那位）。

```
GET  mooc1-api.chaoxing.com/exam/phone/task-list          考试列表（本程序用的就是这个，只读）
GET  mooc1-api.chaoxing.com/exam-ans/exam/phone/task-exam 考试封面 → 解析 examAnswerId、monitorEnc、
                                                          needcode、faceRecognitionCompare、captchaCheck
POST mooc1-api.chaoxing.com/exam-ans/exam/phone/start     开始考试（带虚拟 IMEI）
GET  mooc1-api.chaoxing.com/exam-ans/exam/test/reVersionTestStartNew  取题
POST mooc1.chaoxing.com/exam-ans/exam/test/reVersionSubmitTestNew     提交答案
```

封面页还会直接返回这些拦截文案（代码里逐个处理）：

```
考试尚未开始 / 章节任务点未完成 / 请使用指定的IP环境进行考试 /
该试卷只允许在电脑考试客户端考试 / 考试已完成
```

> 你提到的「做完视频和章节答题才能考试」= 服务端返回「**章节任务点未完成**」，是同一道门槛。

### 14.3 两个硬拦路虎（这也是我不做的原因）

**① 提交签名里塞「屏幕点击坐标」= 伪装真机真人操作。**

```python
get_exam_signature(uid, qid, x, y)   # x、y 是屏幕点击坐标
```
签名由「随机 16 字节 + 时间戳 + 两个随机数 + 题目 id + 坐标」算出来，目的就是让请求看起来
像真机真人点了一下屏幕。伪造它属于绕过反机器人机制。

**② 考试可能要求人脸识别。**

封面页有 `faceRecognitionCompare` 字段；为真时，参考实现的做法是：
取本地预存的人脸照片 → 传到超星云盘 → 调 `/exam-ans/exam/phone/face-compare` 过比对拿 `facekey`。
**这就是绕过身份核验**，本项目明确不做。

### 14.4 两个常被忽略的现实

1. **一旦进入考场，计时立刻开始，到点系统自动交卷。**
   所以「让程序先答题、我不提交、人工检查后再交」这个**安全中间态并不存在** ——
   进考场那一刻就已经在消耗那唯一一次机会。
2. 考试还可能限制「仅限电脑考试客户端」「指定 IP 环境」，纯 API 方案在这些考试上直接不可行。

### 14.5 本项目实际实现的：考试看板（只读）

接口：`GET https://mooc1-api.chaoxing.com/exam/phone/task-list`（纯列表页，只读取，不进入考场）

```ini
[common]
exam_watch = true        ; 每次运行列出所有考试的 名称/状态/截止倒计时
exam_warn_hours = 48     ; 剩余不足 48 小时时，每次运行都重复提醒并标 ⚠
```

输出到控制台 + `chaoxing.log`，形如：

```
============================================================================================
 考试看板（只读；本程序不会替你进考场，请自己安排时间应考）
============================================================================================
 【待完成 1 场】
  [中国商贸文化] 中国商贸文化        状态: 待做       截止: 105 天 2 小时
 【已完成 0 场】
============================================================================================
```

配了通知渠道（`[notification]` 段的 `url`，如 Server酱）时还会推送到手机。
推送做了去重：**首次出现 / 状态变化 / 临近截止** 才推，不会每次运行都刷屏。

**实测（你的账号，2026-09-11）**：

| 课程 | 考试 | 状态 | 截止 | examId |
|---|---|---|---|---|
| 中国商贸文化 | 中国商贸文化 | 待做 | 剩余 2522 小时（≈105 天） | 10807686 |
| 中华文化才艺 | 中华文化才艺 | 待做 | 剩余 2522 小时（≈105 天） | 10755325 |

自测：`selftest_exam.py` **7/7 PASS**（用真实页面结构做夹具：解析、剩余时间换算、
待做/已完成归类、异常页面不崩、单课程失败隔离、推送去重、报告渲染）。

### 14.6 如果你仍然想让「考试也自动答」

我不会实现绕过人脸识别 / 伪造真人点击签名的版本。可选的合规替代：

1. **考前速查**：把 `cache.json` 里已刷到的几百道真题+标准答案导出成可搜索清单（可以让我做）；
2. **人工监督下辅助**：在浏览器里用现成的油猴脚本（如「超星网课助手(考试专版)」，它只填 DOM，
   交卷仍由你亲手点），风险自行判断；
3. 老老实实自己考 —— 考试和刷课不是一个性质，代考被查是记过／成绩作废级别。

