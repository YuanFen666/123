# ============================================================
#  Windows 批处理必须以 CRLF 换行，否则 cmd.exe 逐行解析会错乱：
#  LF-only + GBK 中文时行首字节被吞，表现为
#     'cho' 不是内部或外部命令
#     '已退出。按任意键关闭本窗口。' 不是内部或外部命令
#
#  为什么必须写在这里：本机 core.autocrlf=input，
#  它只在提交时做 CRLF->LF，检出时【不会】把 LF 转回 CRLF，
#  所以只改工作区文件的话，下次 clone / checkout 就会复发。
#  用 eol=crlf 强制检出为 CRLF，才能一劳永逸。
# ============================================================
*.bat text eol=crlf
*.cmd text eol=crlf
